from typing import List

class Solution:
    # Using Linear Search 
    def searchInsert(self, nums: List[int], target: int) -> int:
        for i in range(len(nums)):
            if target <= nums[i]:
                return i
        return len(nums)
Ob = Solution()
print(Ob.searchInsert([1,3,5,7], 2))